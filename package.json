{
  "name": "prismian",
  "version": "0.1.0",
  "private": true,
  "description": "Prismian — AI-safe access layer for teams (monorepo root)",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "test": "turbo run test",
    "test:api": "turbo run test:api",
    "lint": "turbo run lint",
    "db:migrate": "turbo run db:migrate --filter=@prismian/api"
  },
  "keywords": [
    "knowledge-base",
    "mcp",
    "ai",
    "team-collaboration"
  ],
  "author": "",
  "license": "Apache-2.0",
  "packageManager": "pnpm@9.15.4",
  "devDependencies": {
    "turbo": "^2.9.6"
  }
}
